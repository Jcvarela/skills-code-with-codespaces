name: Regex Title Extractor

on:
  workflow_dispatch:
    inputs:
      input_string:
        description: "Enter the string"
        required: true
        type: string

# ✅ run-name must be at root level
run-name: "Run for ${{ replace(github.event.inputs.input_string, 'TASK-', '') }}"

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Print Input
        run: echo "Input was"
