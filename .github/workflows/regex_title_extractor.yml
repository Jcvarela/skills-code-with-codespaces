name: Regex Title Extractor

on:
  workflow_dispatch:
    inputs:
      input_string:
        description: "Enter the string"
        required: true
        type: string

# ✅ run-name must be at root level
run-name: >
  ${{ inputs.input_string[5] && 'length >= 5' || 'length < 6' }}

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Print Input
        run: echo "Input was ${{ inputs.input_string }}"
