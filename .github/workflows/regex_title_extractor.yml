name: Regex Title Extractor

on:
  workflow_dispatch:
    inputs:
      input_string:
        description: "Enter the string"
        required: true
        type: string

# ✅ run-name must be at root level
run-name: ${{ contains(github.event.inputs.input_string, 'feature') && 'Run for Feature Branch' || 'Run for Other Branch' }}

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Print Input
        run: echo "Input was"
